const input = document.getElementById('file-input');
const inputButton = document.getElementById('file-input-button');
const card = document.getElementById('file-card');
const fileName = document.getElementById('file-name');
const cancel = document.getElementById('file-cancel');
const addKey = document.getElementById('add-key-textfield');
const addValue = document.getElementById('add-value-textfield');
const addButton = document.getElementById('add-button');
const removeKey = document.getElementById('remove-key-textfield');
const removeButton = document.getElementById('remove-button');
const morphileButton = document.getElementById('morphile-button');
const downloadButton = document.getElementById('download-metadata-button');